name: Docker

on:
  push:
    tags: ["v*"]

jobs:
  publish:
    uses: matt-riley/matt-riley-ci/.github/workflows/docker-ghcr-publish.yml@4ff76b1a606b1fbba163119a65bf7b05f91ab9b6 # v3
    with:
      tag-name: ${{ github.ref_name }}
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

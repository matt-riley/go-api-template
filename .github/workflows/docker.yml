name: Docker

on:
  push:
    tags: ["v*"]

jobs:
  publish:
    uses: matt-riley/matt-riley-ci/.github/workflows/docker-ghcr-publish.yml@2aa90cd79663d10959d67faf89a3ecce29392f6a # v2
    with:
      tag-name: ${{ github.ref_name }}
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
